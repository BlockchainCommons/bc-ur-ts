# Logs
*.log
npm-debug.log*

# Dependencies
node_modules

# Build artifacts
dist
coverage
docs
*.tsbuildinfo

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.sw?

# OS
.DS_Store

# Env
.env
.env.*

# Rust cross-validation harness build artifacts
tests/rust-validation/target

# Lockfile: intentionally absent until every @blockchaincommons dependency is
# published. Local verification installs siblings with file:../ links, which
# would bake unpublishable paths into bun.lock. A real lockfile is committed at
# the first release.
bun.lock
